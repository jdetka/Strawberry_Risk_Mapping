
from pathlib import Path

# Repository root
REPO_ROOT = Path(r"D:\GitHub\Strawberry_Risk_Mapping")

# Data folders
DATA_DIR = REPO_ROOT / "data"
PROCESSED_DATA = DATA_DIR / "processed"
DERIVED_DATA = DATA_DIR / "derived"

# Model Directory
MODEL_DIR = FIELD_OUTPUT_DIR / "rsf_models"

# Output folders
OUTPUT_DIR = REPO_ROOT / "output"
GRAPHICS_DIR = REPO_ROOT / "graphics"
FIELD_OUTPUT_DIR = REPO_ROOT / "output"
RISK_EXPORT_DIR = FIELD_OUTPUT_DIR / "rsf_risk_exports"


# Project settings
SITE_CODE = "ERL"
FIELDS = [1, 2, 3]

